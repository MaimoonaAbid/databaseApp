// 'use strict';

// module.exports = {
//   up: async (queryInterface, Sequelize) => {
//     // Add the db_name column to the Users table
//     await queryInterface.addColumn('Users', 'db_name', {
//       type: Sequelize.STRING, // Define the data type for db_name
//       allowNull: true, // Specify if the column can be null
//     });
//   },

//   down: async (queryInterface, Sequelize) => {
//     // Remove the db_name column from the Users table if needed
//     await queryInterface.removeColumn('Users', 'db_name');
//   },
// };
